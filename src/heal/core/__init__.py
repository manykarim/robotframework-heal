"""Pure healing engine: schemas, pipeline, agents, failure-class plugins.

This package MUST NOT import `robot` or any automation library
(Browser, AppiumLibrary, selenium). Enforced by tests/unit/test_import_boundaries.py.
"""
