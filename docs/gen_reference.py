"""Build-time reference generation (mkdocs-gen-files hook).

Filled in by task 2.2 — generates the configuration and CLI reference pages
from `HealSettings` and the Typer app. Stub keeps the build green meanwhile.
"""
